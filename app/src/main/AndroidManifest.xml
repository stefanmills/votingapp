<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.votingapp">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".Splash" android:theme="@style/Theme.AppCompat.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:theme="@style/Theme.AppCompat.Light.NoActionBar" android:name=".VotePage">

        </activity>
        <activity
            android:name=".ViewResultPage"
            android:parentActivityName=".VotePage">

        </activity>

        <activity
            android:name=".President"
            android:parentActivityName=".VotePage">

        </activity>

        <activity
            android:name=".SecretaryPage"
            android:parentActivityName=".President"></activity>
        <activity
            android:name=".Financial"
            android:parentActivityName=".SecretaryPage"></activity>
        <activity
            android:name=".WomensCommissioner"
            android:parentActivityName=".Financial"></activity>
        <activity android:name=".SelectedVotes"></activity>


    </application>

</manifest>